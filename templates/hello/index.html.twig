<!DOCTYPE html>
<html lang="ja">
<head>
    <meta charset="UTF-8">
    <title>{{ title }}</title>
</head>
<body>
    <h1>{{ title }}</h1>
    <p>{{ message }}</p>
    <form action="/other" method="post">
        <input type="text" name="input">
        <input type="submit" value="Click">
    </form>
</body>
</html>
